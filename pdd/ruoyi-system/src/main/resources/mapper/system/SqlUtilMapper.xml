<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="com.ruoyi.code6g.sqlUtil.mapper.SqlUtilMapper">
	<!--
	<insert id="insert" parameterType="SqlUitl" >
		#{sql}
	</insert>

	<delete id="delete" parameterType="SqlUitl" >
		#{sql}
	</delete>
	<update id="update" parameterType="SqlUitl" >
		#{sql}
	</update>
	-->

	<select id="list" resultType="map">
		${_parameter}
	</select>

	<insert id="insert"   >
		${_parameter}
	</insert>

	<delete id="delete" >
		${_parameter}
	</delete>
	<update id="update"  >
		${_parameter}
	</update>


</mapper>

